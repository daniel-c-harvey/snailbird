<div>
    <textarea class="form-control" type="text" @bind-value="@ViewModel.RawText" @bind-value:event="oninput" />
</div>

@if (PreviewMarkup)
{
    <hr />
    <MarkupParagraph ViewModel="@ViewModel.ParagraphViewModel" />
}